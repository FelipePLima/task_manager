<div class="row">
  <div class="well col-md-12">
    <h3>Minhas Listas de Tarefas</h3>
    <table class="table table-hover">
      <thead>
        <tr>
          <th width="40%">Nome</th>
          <th width="10%">Publica?</th>
          <th width="10%">Fechada?</th>
          <th width="40%">Usuário</th>
          <th width="10%"></th>
        </tr>
      </thead>
      <tbody>
        <% @task_lists.each do |task_list| %>
          <tr>
            <td><%= task_list.name %></td>
            <td><%= task_list.is_public %></td>
            <td><%= task_list.done %></td>
            <td><%= task_list.user.email %></td>
            <td><%= finish_task_link(task_list)%></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
  <%= link_to 'Nova Lista', new_task_list_path, class: "btn btn-primary" %>
</div>
